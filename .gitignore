*.sqlite
*.sqlite3*
*.db
*.csv
*.db-shm
*.db-wal
*.txt

# Ignore leaked secrets results directory and files (root and module-specific)
leaked_secrets_results/
*/leaked_secrets_results/
scan_logs/
*/scan_logs/
logs/
*/logs/
verified_secrets*

# Analyzer results (contains sensitive data)
analyzer/analyzed_results/
*/analyzed_results/

# Python cache files
__pycache__/
*/__pycache__/
*.pyc
*.pyo

# Scan state files (runtime state, not needed for script execution)
scan_state.json
*/scan_state.json

# Configuration files (now in config/ directory)
config/mailgun_config.sh
config/telegram_config.sh
config/discord_config.sh
# Template files are tracked in git